section.skills#skills
  .skills__head
    h2.skills__header Чем я могу быть полезен вам
    p.skills__description
      | Этот сайт я сделал в рамках обучения в Школе онлайн образования #[br]
      | LoftSchool. Чуть позже я освежу в нём свой контент. А пока посмотрите, #[br]
      | как тут всё классно и красиво!

  .skills__container
    .hands-keyboard
      +image('hands.png', 'hands-keyboard')

    .wrap-skills
      svg(viewBox="0 0 0 0" height="0" width="0")
        linearGradient(id="gradient" x1="0%" y1="100%" x2="100%" y2="0%")
          stop(offset="0%" stop-color="#d0721b")
          stop(offset="0%" stop-color="#DC9322")

      .skills-block
        h3.skills-block__head Front-end
        - const percent1 = 80  
        - const frontend_arr= ['HTML5', 'CSS3', 'Javascript', 'Vue.js']
        ul.skills-block__list
          - for(let i = 1; i < frontend_arr.length+1; i++)
            li.svgli
                svg(viewBox="0 0 100 100" height="136" width="136").svgls
                  circle(r="40" cx="50%" cy="50%" ).svgsircle.under-layer
                  circle(r="40" cx="50%" cy="50%" stroke="url(#gradient)" transform="rotate(180 50 50)" style=`stroke-dasharray: ${(percent1-(i * 10))*5} 282.6`).svgsircle.above-layer
                .skill__title #{frontend_arr[i-1]}

      .skills-block
        h3.skills-block__head Back-end
        - const percent2 = 80  
        - const backend_arr= ['PHP', 'Wordpress', 'MySQL', 'Laravel']
        
        ul.skills-block__list
          - for(let i = 1; i < backend_arr.length+1; i++)
            li.svgli
                svg(viewBox="0 0 100 100" height="136" width="136").svgls
                  circle(r="40" cx="50%" cy="50%" ).svgsircle.under-layer
                  circle(r="40" cx="50%" cy="50%" stroke="url(#gradient)" transform="rotate(180 50 50)" style=`stroke-dasharray: ${(percent2-(i * 10))*5} 282.6`).svgsircle.above-layer
                .skill__title #{backend_arr[i-1]}

      .skills-block
        h3.skills-block__head Workflow
        - const percent3 = 80  
        - const workflow_arr= ['Gulp', 'Terminal', 'Git', 'Webpack']
        ul.skills-block__list
          - for(let i = 1; i < workflow_arr.length+1; i++)
            li.svgli
                svg(viewBox="0 0 100 100" height="136" width="136").svgls
                  circle(r="40" cx="50%" cy="50%" ).svgsircle.under-layer
                  circle(r="40" cx="50%" cy="50%" stroke="url(#gradient)" transform="rotate(180 50 50)" style=`stroke-dasharray: ${(percent3-(i * 10))*5} 282.6`).svgsircle.above-layer
                .skill__title #{workflow_arr[i-1]}
